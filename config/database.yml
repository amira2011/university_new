########################
default: &default
  adapter: mysql2
  encoding: utf8mb4
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: abid
  password: password
  host: 127.0.0.1

development:
  <<: *default
  database: university_development

test:
  <<: *default
  database: university_test

production:
  <<: *default
  database: university_production
  username: abid
  password: password
  host: your_mysql_production_host